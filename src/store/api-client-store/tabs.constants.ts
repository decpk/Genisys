/**
 * localStorage key prefix for persisted API Client request tabs.
 * The active workspace id is appended to scope tabs per workspace.
 */
export const API_CLIENT_TABS_STORAGE_PREFIX = 'genisys:apiClient:tabs:v1:'
