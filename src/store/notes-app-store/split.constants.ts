/** Minimum fraction the first pane may occupy in a split. */
export const NOTES_SPLIT_MIN_RATIO = 0.2

/** Maximum fraction the first pane may occupy in a split. */
export const NOTES_SPLIT_MAX_RATIO = 0.8

/** Default divider position when a split is opened or reset. */
export const NOTES_SPLIT_DEFAULT_RATIO = 0.5
