import type { PanelAIConfig } from './panel-ai-config.types'

export const DEFAULT_PANEL_AI_CONFIG: PanelAIConfig = {
  enableTools: true,
  maxTools: 128,
  enableRepoTools: true,
  enableMcpTools: true,
}
