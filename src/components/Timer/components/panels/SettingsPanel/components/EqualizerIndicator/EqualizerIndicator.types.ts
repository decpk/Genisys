export interface EqualizerIndicatorProps {
  /** Optional className override. */
  className?: string
  /** Size in pixels for the square container. Default 14 (matches lucide Play size 12). */
  size?: number
}
