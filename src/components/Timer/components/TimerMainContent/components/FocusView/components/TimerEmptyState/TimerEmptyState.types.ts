export interface TimerEmptyStateProps {
  /**
   * Optional className appended to the root container so callers can
   * override layout (e.g. tighter padding for a mini surface).
   */
  className?: string
}
