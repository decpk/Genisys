import type { SectionVariant } from '../constants/sectionVariants.constants'

export interface SectionIconChipProps {
  /** Drives chip background gradient, ring, and child icon color. */
  variant: SectionVariant
}
