/** Tailwind class-name constants for the Previewer root layout. */
export const STYLES = {
  root: 'h-full w-full flex flex-col bg-background text-foreground overflow-hidden',
  stage: 'flex-1 min-h-0',
} as const
