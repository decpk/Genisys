/** Tailwind class-name constants for the `BookmarkImportFooter`. */
export const STYLES = {
  root: 'flex flex-col gap-3',
  field: 'flex flex-col gap-1.5',
  label: 'text-xs font-medium text-muted-foreground',
  pickerTrigger: 'w-full justify-between',
  pickerName: 'truncate',
  toggle: 'flex items-center gap-2 cursor-pointer select-none',
  toggleText: 'text-xs text-foreground',
} as const
