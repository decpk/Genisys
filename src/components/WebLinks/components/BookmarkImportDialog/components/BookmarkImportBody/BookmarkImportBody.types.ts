import type { BookmarkImportDialogViewModel } from '../../BookmarkImportDialog.types'

/** Body props mirror the dialog view-model (spread in by the parent dialog). */
export type BookmarkImportBodyProps = BookmarkImportDialogViewModel
