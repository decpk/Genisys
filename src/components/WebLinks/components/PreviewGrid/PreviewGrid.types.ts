import type { SavedPreview } from '@/components/WebLinks/WebLinks.types'

/** Props for the responsive saved-preview grid. */
export interface PreviewGridProps {
  /** Previews to render as cards. */
  previews: SavedPreview[]
}
