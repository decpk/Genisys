import type { ScreenshotImportDialogViewModel } from '../../ScreenshotImportDialog.types'

/** Body props mirror the dialog view-model (spread in by the parent dialog). */
export type ScreenshotImportBodyProps = ScreenshotImportDialogViewModel
