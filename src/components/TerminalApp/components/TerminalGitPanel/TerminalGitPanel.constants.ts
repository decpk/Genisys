/** Minimum draggable width (px) of the terminal git changes panel. */
export const TERMINAL_GIT_PANEL_MIN_WIDTH = 240

/** Maximum draggable width (px) of the terminal git changes panel. */
export const TERMINAL_GIT_PANEL_MAX_WIDTH = 560
