export const STEP_THEME_STYLES = {
  root: 'flex flex-col items-center w-full max-w-[860px]',
  heading: 'text-[2rem] font-bold tracking-tight text-foreground text-center',
  subheading: 'text-sm text-muted-foreground/60 mt-2 text-center',
  tabBar:
    'flex items-center gap-1 mt-8 p-1 rounded-full bg-muted/40 border border-border/40 backdrop-blur-sm',
  grid: 'w-full mt-8 grid grid-cols-5 gap-4',
} as const
