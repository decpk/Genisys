export const conversationViewStyles = {
  // `relative` so the floating MessageComposer can absolutely position itself
  // over the message list.
  root: 'relative flex h-full flex-col bg-gradient-to-b from-background to-muted/20',
} as const
