/**
 * Default display title used when a note has no title set yet.
 * Kept in sync with the empty-state placeholder used in the editor toolbar.
 */
export const UNTITLED_NOTE_FALLBACK = 'Untitled'

/**
 * Default display title used when a container (project / notebook / section /
 * topic) somehow has no `name` set.
 */
export const UNTITLED_CONTAINER_FALLBACK = 'Untitled'

/**
 * Separator placed between breadcrumb segments in chapter titles
 * (e.g. `Notebook / Section / Topic`).
 */
export const BREADCRUMB_SEPARATOR = ' / '

/**
 * Separator placed between the breadcrumb path and the note title.
 * (e.g. `Section / Topic — Note Title`).
 */
export const BREADCRUMB_TITLE_SEPARATOR = ' — '
