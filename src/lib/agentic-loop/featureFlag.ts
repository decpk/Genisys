/**
 * Feature flag controlling whether the runners route through the new
 * shared agentic-loop primitive. Flip to `false` to fall back to the
 * legacy in-runner loop.
 */
export const USE_NEW_AGENTIC_LOOP = true
