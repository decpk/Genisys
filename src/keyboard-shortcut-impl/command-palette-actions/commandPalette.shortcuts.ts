import type { ShortcutDef } from '@/frameworks/keyboard-shortcut/KeyboardShortcut.types'

/**
 * The two Command Palette shortcuts are declared inside `GLOBAL_SHORTCUTS` so
 * they appear in the Settings UI's "General" category. This file is kept as a
 * placeholder for future palette-scoped shortcuts and to mirror the per-feature
 * registration pattern used by other action modules.
 */
export const COMMAND_PALETTE_SHORTCUTS: ShortcutDef[] = []
