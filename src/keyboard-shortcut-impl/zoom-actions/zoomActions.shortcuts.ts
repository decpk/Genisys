import type { ShortcutDef } from '@/frameworks/keyboard-shortcut/KeyboardShortcut.types'

/**
 * The zoom shortcut definitions live in `GLOBAL_SHORTCUTS` (so they appear in
 * the Settings UI's "View" category alongside other view shortcuts). This file
 * is kept to mirror the per-feature registration pattern.
 */
export const ZOOM_ACTIONS_SHORTCUTS: ShortcutDef[] = []
